% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/likelihood.R
\name{neg_loglik_slt}
\alias{neg_loglik_slt}
\title{Case-aware negative log-likelihood (optimiser objective)}
\usage{
neg_loglik_slt(log_par, family, case, data, tau1)
}
\arguments{
\item{log_par}{Numeric vector, \code{log()} of the natural-scale case
parameters (ensures positivity without constrained optimisation).}

\item{family}{A family key (see \code{\link[=slt_families]{slt_families()}}) or family object.}

\item{case}{One of \code{"common"}, \code{"shape1"}, \code{"shape11"}.}

\item{data}{A list with numeric vectors \code{Y} (stage-0 observed failure
times) and \code{Z} (stage-1 observed failure times), as returned by
\code{\link[=rslt]{rslt()}}.}

\item{tau1}{The stage-change time used to shift \code{Z} back to the
stage-1 time origin.}
}
\value{
A finite scalar (never \code{-Inf}/\code{Inf}/\code{NaN}), suitable as an
\code{\link[stats:optim]{stats::optim()}} objective.
}
\description{
Maps a log-scale, case-specific parameter vector to \verb{(theta0, theta1)}
via \code{slt_expand_params()} and returns the negative log-likelihood, with any
internal error (e.g. from the Lambert-W-based Power Lindley quantile
straying outside \verb{[0, 1]} during a finite-difference gradient step) or
non-finite result converted to a large, finite penalty so that
\code{\link[stats:optim]{stats::optim()}} never aborts.
}
\keyword{internal}
