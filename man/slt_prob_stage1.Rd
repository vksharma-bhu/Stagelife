% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/design-theory.R
\name{slt_prob_stage1}
\alias{slt_prob_stage1}
\title{Probability of observing at least one stage-1 failure (Laumen & Cramer, Lemma 4.1)}
\usage{
slt_prob_stage1(
  n,
  tau1,
  theta0,
  family = "weibull",
  design = c("typeP", "typeM"),
  pi1 = NULL,
  R0_1 = NULL
)
}
\arguments{
\item{n}{Number of units placed on test.}

\item{tau1}{Stage-change time.}

\item{theta0}{Numeric \code{c(lambda0, alpha0)}, baseline-stage parameters.}

\item{family}{A family key (see \code{\link[=slt_families]{slt_families()}}) or family object.}

\item{design}{\code{"typeP"} (fixed target proportion \code{pi1}) or \code{"typeM"}
(fixed target count \code{R0_1}), matching \code{\link[=rslt]{rslt()}}.}

\item{pi1}{Target proportion transferred, for \code{design = "typeP"}.}

\item{R0_1}{Target number transferred, for \code{design = "typeM"}.}
}
\value{
A list with \code{n_star} (the threshold on \code{D1}) and \code{prob}
(\verb{P(R*_1 > 0)}).
}
\description{
Computes the probability of observing at least one stage-1 failure,
\verb{P(R*_1 > 0)}, implementing Lemma 4.1 of Laumen & Cramer (2019).
}
\details{
Because the maximum likelihood estimate of the elevated-stage parameter
only exists when at least one unit is actually transferred to stage 1
and fails there (\verb{R*_1 > 0}), this is a useful quantity for planning a
stage life test: choosing \code{n}, \code{tau1}, and \code{pi1}/\code{R0_1} so that
\code{slt_prob_stage1()} is comfortably close to 1.

\verb{R*_1 = rho(D1) > 0} iff \verb{D1 <= n*}, where \verb{n* = floor(n - 1/pi1)} for
the \code{"typeP"} design and \verb{n* = n - 1} for the \code{"typeM"} design (with
\code{R0_1 >= 1}), and \code{D1 ~ Binomial(n, F0(tau1))}.
}
\examples{
# Reproduces Table 5 of Laumen & Cramer (2019) exactly (n = 12,
# theta0 = 1 i.e. lambda0 = 1, tau1 = 0.5):
slt_prob_stage1(12, 0.5, c(1, 1), design = "typeP", pi1 = 0.25)  # n*=8,  prob=0.9864756680
slt_prob_stage1(12, 0.5, c(1, 1), design = "typeM", R0_1 = 6)    # n*=11, prob=0.9999862301
}
