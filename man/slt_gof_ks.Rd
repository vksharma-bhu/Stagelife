% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gof.R
\name{slt_gof_ks}
\alias{slt_gof_ks}
\title{Kolmogorov-Smirnov goodness-of-fit test with parametric-bootstrap correction for a fitted two-stage life testing model}
\usage{
slt_gof_ks(fit, B = 1000, n_cores = 1L)
}
\arguments{
\item{fit}{A converged \code{"slt_fit"} object, fit to data from a
simple-step-stress (\code{\link[=rslt]{rslt()}} with \code{prop_transfer = 1}) design.}

\item{B}{Number of parametric bootstrap replicates.}

\item{n_cores}{Number of cores for \code{\link[parallel:mclapply]{parallel::mclapply()}}.}
}
\value{
An object of class \code{"slt_gof"} with elements \code{D}
(KS statistic), \code{p_naive}, \code{p_bootstrap}, \code{B}, \code{B_converged}.
}
\description{
Performs the Kolmogorov-Smirnov goodness-of-fit test and reports both
the naive p-value (for reference) and the standard
parametric-bootstrap-corrected p-value.
}
\details{
The classical KS null distribution assumes a fully specified (not
estimated) reference distribution; using it when the parameters were
estimated from the same data makes the naive p-value anti-conservative
(too small), a problem first noted by Lilliefors. The parametric
bootstrap correction simulates \code{B} fresh data sets of the same size
from the fitted model via \code{\link[=rslt]{rslt()}} (at the MLE), refits each with the
same family/case, and takes the corrected p-value to be the
proportion of bootstrap KS statistics at least as extreme as the one
observed on the real data.

This test assumes every survivor was transferred at \code{tau1} (the
classical simple-step-stress design, \code{prop_transfer = 1} in \code{\link[=rslt]{rslt()}}),
because the single pooled reference distribution it uses (\code{\link[=pslt]{pslt()}}) is
only the correct marginal CDF of the \emph{combined} \code{Y}/\code{Z} sample in that
case. Under genuine partial withdrawal, units that survive \code{tau1} but
are \emph{not} transferred keep failing on stage 0 unmodified, which
\code{\link[=pslt]{pslt()}} does not represent -- pooling them into one KS statistic
against \code{\link[=pslt]{pslt()}} would be a biased test. If \code{fit$data} came from a
partial-withdrawal \code{\link[=rslt]{rslt()}} call (\code{prop_transfer < 1} or
\code{design = "typeM"}), this function is not appropriate; a Y-only and a
Z-only KS test against their own (design-invariant) marginals would be
the correct generalisation, but is not currently implemented.
}
\examples{
\donttest{
set.seed(1)
d <- rslt(150, "weibull", c(0.05, 1.5), c(0.2, 1.5), tau1 = 5, prop_transfer = 1)
fit <- fit_slt(d, "weibull", "shape1", tau1 = 5)
slt_gof_ks(fit, B = 100)
}
}
