% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/confint.R
\name{slt_bootstrap_ci}
\alias{slt_bootstrap_ci}
\title{Bootstrap confidence intervals for a fitted two-stage life testing model}
\usage{
slt_bootstrap_ci(
  fit,
  B = 1000,
  level = 0.95,
  method = c("nonparametric", "parametric"),
  design = c("typeP", "typeM"),
  prop_transfer = 1,
  R0_1 = NULL,
  n_cores = 1L
)
}
\arguments{
\item{fit}{A converged \code{"slt_fit"} object.}

\item{B}{Number of bootstrap replicates.}

\item{level}{Confidence level.}

\item{method}{\code{"nonparametric"} (default) or \code{"parametric"}.}

\item{design}{For \code{method = "parametric"}, the design used to simulate
new data via \code{\link[=rslt]{rslt()}}: \code{"typeP"} (fixed target proportion, the
default) or \code{"typeM"} (fixed target count).}

\item{prop_transfer}{Proportion transferred, for \code{design = "typeP"}.
Defaults to \code{1} (every survivor transferred, the classical
simple-step-stress design, matching a typical published step-stress
data set); set to the actual value used in your experiment if it was
a genuine partial-withdrawal design.}

\item{R0_1}{Target number transferred, for \code{design = "typeM"}.}

\item{n_cores}{Number of cores for \code{\link[parallel:mclapply]{parallel::mclapply()}} (ignored on
Windows, where replicates run sequentially).}
}
\value{
An object of class \code{"slt_boot_ci"}: a list with \code{percentile},
\code{boot_t}, and \code{log_boot_t} data.frames (columns \code{lower}, \code{upper},
one row per parameter), \code{B}, \code{B_converged}, \code{method}, \code{design},
\code{level}, and the raw \code{boot_estimates} matrix.
}
\description{
Computes percentile, bootstrap-t, and log-scale bootstrap-t confidence
intervals via either non-parametric or a parametric bootstrap.
}
\details{
\strong{Non-parametric} bootstrap resamples (case resampling) the observed
\code{Y}/\code{Z} failure times; \strong{parametric} bootstrap simulates fresh data
from the fitted model at the MLE. The parametric bootstrap is
generally more efficient when the fitted model is (approximately)
correctly specified -- see \code{\link[=slt_gof_ks]{slt_gof_ks()}} -- but is more sensitive to
misspecification; the non-parametric bootstrap is robust to
misspecification but, for small samples, can only reproduce
combinations of already-observed values.

The plain (natural-scale) bootstrap-t interval, like the asymptotic
Wald interval from \code{confint(..., type = "normal")}, is an ordinary
linear pivot (\verb{estimate -/+ quantile * se}) and can occasionally
produce a negative lower bound for a positive parameter, especially
at small \code{n} or for a skewed bootstrap distribution. The log-scale
bootstrap-t interval applies the same pivot construction on
\code{log(theta)} and exponentiates the result, which guarantees a
positive interval by construction -- directly analogous to why
\code{confint(..., type = "lognormal")} (the default) is generally
preferred over \code{type = "normal"} for the asymptotic interval. The
percentile interval is always positive already (it is built from
quantiles of the always-positive bootstrap estimates themselves), so
only bootstrap-t needed a log-scale counterpart.
}
\examples{
\donttest{
set.seed(1)
d <- rslt(150, "weibull", c(0.05, 1.5), c(0.2, 1.5), tau1 = 5, prop_transfer = 1)
fit <- fit_slt(d, "weibull", "shape1", tau1 = 5)
slt_bootstrap_ci(fit, B = 100)
}
}
