% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stage-structure.R
\name{slt_cases}
\alias{slt_cases}
\title{Identifiable parameter-sharing cases for two-stage estimation}
\usage{
slt_cases()
}
\value{
A data.frame with columns \code{case}, \code{n_par}, \code{parameters},
\code{description}.
}
\description{
Describes the three parameter-sharing structures between the
baseline-stage parameters \verb{theta0 = (lambda0, alpha0)} and the
elevated-stage parameters \verb{theta1 = (lambda1, alpha1)} supported for
estimation. A fourth, fully unrestricted structure (\code{alpha0} and
\code{alpha1} both free) is deliberately not supported; see Details.
}
\details{
Four parameter-sharing structures are logically possible for the pair
\eqn{\theta_0=(\lambda_0,\alpha_0)}, \eqn{\theta_1=(\lambda_1,\alpha_1)}:
\deqn{\begin{array}{lll}
  \texttt{full}    : & \theta_0=(\lambda_0,\alpha_0), & \theta_1=(\lambda_1,\alpha_1)
                       \quad\text{(4 free parameters)} \\
  \texttt{common}  : & \theta_0=(\lambda_0,\alpha),   & \theta_1=(\lambda_1,\alpha)
                       \quad\text{(3 free parameters, shared shape)} \\
  \texttt{shape1}  : & \theta_0=(\lambda_0,\alpha_0), & \theta_1=(\lambda_1,1)
                       \quad\text{(3 free parameters)} \\
  \texttt{shape11} : & \theta_0=(\lambda_0,1),        & \theta_1=(\lambda_1,1)
                       \quad\text{(2 free parameters)}
\end{array}}
This package supports the last three; \code{full} is deliberately
\strong{not} offered (see "Why \code{full} is excluded" below).

\code{shape1}/\code{shape11} fix the elevated-stage shape to the \emph{numeric
value} 1 -- this is a structural constraint, not a claim that the
elevated stage becomes Exponential. The two only coincide for the
four families whose \code{alpha = 1} boundary happens to be Exponential
(\code{weibull}, \code{expexponential}, \code{moexponential}, \code{gamma}; see
\code{?slt_dist}); for the other eight families, \code{alpha1 = 1} is simply
that family's own (otherwise unnamed) member at shape 1 -- e.g. Chen
with its shape fixed at 1, not an Exponential elevated stage.
Fixing the shape is still meaningful regardless: it
reduces the free parameter count the same way for every family (see
"Why \code{full} is excluded" below), and "is the elevated-stage shape
exactly 1?" is a concrete, testable nested hypothesis via
\code{\link[=slt_lrt]{slt_lrt()}} whether or not that constraint has a textbook name.

Each case defines a bijection between its free-parameter vector \code{par}
(on the natural, positive scale, in the order given by
\code{case_info(case)$par_names}) and \verb{(theta0, theta1)}, implemented by
\code{\link[=slt_expand_params]{slt_expand_params()}} and inverted by \code{\link[=slt_reduce_params]{slt_reduce_params()}}:
\deqn{\texttt{common}:\ (\lambda_0,\lambda_1,\alpha) \mapsto
        \big((\lambda_0,\alpha),\ (\lambda_1,\alpha)\big),}
\deqn{\texttt{shape1}:\ (\lambda_0,\alpha_0,\lambda_1) \mapsto
        \big((\lambda_0,\alpha_0),\ (\lambda_1,1)\big),}
\deqn{\texttt{shape11}:\ (\lambda_0,\lambda_1) \mapsto
        \big((\lambda_0,1),\ (\lambda_1,1)\big).}
\code{fit_slt()} optimises the log-likelihood (\code{\link[=loglik_slt]{loglik_slt()}}, via
\code{\link[=neg_loglik_slt]{neg_loglik_slt()}}) over \code{log(par)} in this order, then reports the
fitted \code{par} under exactly these names -- so, for instance, \code{case = "shape1"}'s second free parameter is always labelled \code{alpha0} and is
always the baseline-stage shape, never accidentally the elevated-stage
rate or any other quantity. This parameter-index bookkeeping (\code{ case_info()$par_names}, \code{\link[=slt_expand_params]{slt_expand_params()}}, \code{\link[=slt_reduce_params]{slt_reduce_params()}},
and \code{fit_slt()}'s internal default-initial-value heuristic all sharing
one consistent order) is exactly the kind of place a silent
mislabelling bug could hide, so it has been checked directly: the
round trip \code{slt_expand_params(slt_reduce_params(theta0, theta1, case), case)} reproduces \verb{(theta0, theta1)} exactly for all three
cases, and a full simulate-then-refit check (large \code{n}, known
\code{theta0}/\code{theta1}) recovers the true values under the correct
parameter names for all three cases (see the package's test suite,
\code{test-fit.R}).

\code{full} (both shapes free) is excluded because, with a single
change-point and no replicated stress levels, its extra free
parameter is empirically non-identifiable (a flat likelihood ridge,
frequent non-convergence); \code{shape1}/\code{shape11} remove this by fixing
one or both shapes to 1, \code{common} by tying them together instead.
\code{shape11} is nested in both \code{common} and \code{shape1} (the natural null
model for \code{\link[=slt_lrt]{slt_lrt()}}); \code{shape1} and \code{common} are not nested in each
other, so compare those two with \code{\link[=slt_model_comparison]{slt_model_comparison()}} instead.
}
\examples{
slt_cases()
}
\seealso{
\code{\link[=slt_expand_params]{slt_expand_params()}} and \code{\link[=slt_reduce_params]{slt_reduce_params()}} for the
parameter mapping implementing each case; \code{\link[=fit_slt]{fit_slt()}} for
estimation; \code{\link[=slt_lrt]{slt_lrt()}} for the nested likelihood-ratio test;
\code{\link[=slt_model_comparison]{slt_model_comparison()}} for comparing non-nested cases.
}
