% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{is_failed_replicate}
\alias{is_failed_replicate}
\alias{drop_failed_replicates}
\title{Identify a failed Monte Carlo / bootstrap replicate}
\usage{
is_failed_replicate(x)

drop_failed_replicates(res)
}
\arguments{
\item{x}{A single replicate result.}

\item{res}{A list of replicate results (e.g. from \code{\link[=lapply]{lapply()}} or
\code{\link[parallel:mclapply]{parallel::mclapply()}}).}
}
\value{
A logical scalar.

\code{drop_failed_replicates()} returns the subset of \code{res} that are
not failures.
}
\description{
A replicate is considered failed if its worker function returned \code{NULL}
(the convention used throughout this package for "this replicate could
not be computed"), or if it is an error/condition object. The latter
case arises specifically when \code{\link[parallel:mclapply]{parallel::mclapply()}} is used: a forked
worker that hits an uncaught error does not propagate the error to the
caller, it returns a \code{"try-error"} object in that slot instead, which
would otherwise silently corrupt downstream aggregation (e.g.
\code{rbind()}-ing a character error message in among numeric estimates).
Every parallel replicate loop in this package (bootstrap, goodness of
fit, simulation studies) filters its results through
\code{\link[=drop_failed_replicates]{drop_failed_replicates()}} for this reason, on top of wrapping each
individual replicate in its own \code{tryCatch()} so a single numerically
fragile draw (most commonly seen with the Power Lindley family; see
\code{vignette("stagelife-intro")}) is skipped rather than aborting the
whole run.
}
\keyword{internal}
