% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gof.R
\name{slt_model_comparison}
\alias{slt_model_comparison}
\title{Compare several fitted models by information criteria}
\usage{
slt_model_comparison(fits, sort_by = c("aic", "bic", "loglik"))
}
\arguments{
\item{fits}{A (preferably named) list of \code{"slt_fit"} objects.}

\item{sort_by}{One of \code{"aic"} (default), \code{"bic"}, \code{"loglik"}.}
}
\value{
A data.frame with columns \code{model}, \code{family}, \code{case}, \code{n_par},
\code{logLik}, \code{AIC}, \code{BIC}, \code{converged}.
}
\description{
Builds a ranked comparison table across any number of fitted models,
by information criteria.
}
\details{
By default sorted by AIC, across any number of \code{"slt_fit"} objects --
typically different \code{family}/\code{case} combinations fit to the same
data set -- for exploratory model selection on real data. This is a
lightweight diagnostic table for a single data set, not a Monte
Carlo simulation study.
}
\examples{
set.seed(1)
d <- rslt(200, "weibull", c(0.05, 1.5), c(0.2, 1.5), tau1 = 5, prop_transfer = 1)
fits <- list(
  "Weibull / shape1"  = fit_slt(d, "weibull", "shape1", tau1 = 5),
  "Weibull / shape11" = fit_slt(d, "weibull", "shape11", tau1 = 5),
  "Weibull / common"  = fit_slt(d, "weibull", "common", tau1 = 5)
)
slt_model_comparison(fits)
}
