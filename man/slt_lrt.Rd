% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gof.R
\name{slt_lrt}
\alias{slt_lrt}
\title{Likelihood-ratio test between nested parameter-sharing cases}
\usage{
slt_lrt(fit_full, fit_reduced)
}
\arguments{
\item{fit_full, fit_reduced}{Converged \code{"slt_fit"} objects for the same
\code{family}, \code{tau1} and \code{data}, with \code{fit_reduced} nested in \code{fit_full}.}
}
\value{
An object of class \code{"slt_lrt"} with \code{statistic}, \code{df},
\code{p.value}.
}
\description{
Tests whether a reduced (nested) parameter-sharing case is adequate
against a less-restricted alternative, via the likelihood-ratio
statistic.
}
\details{
The only nested pairs among the three supported cases are
\verb{("shape1", "shape11")} and \verb{("common", "shape11")} (both fix the
remaining free shape parameter(s) to 1); see \code{\link[=slt_cases]{slt_cases()}}. This
function tests \code{H0}: the reduced (\code{"shape11"}) model is adequate,
against the corresponding less-restricted alternative.
}
\examples{
set.seed(1)
d <- rslt(200, "weibull", c(0.05, 1), c(0.2, 1), tau1 = 5, prop_transfer = 1)
f1 <- fit_slt(d, "weibull", "shape1", tau1 = 5)
f0 <- fit_slt(d, "weibull", "shape11", tau1 = 5)
slt_lrt(f1, f0)
}
