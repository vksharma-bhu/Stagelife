% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{rslt}
\alias{rslt}
\title{Simulate data from the two-stage life-testing model}
\usage{
rslt(
  n,
  family,
  theta0,
  theta1,
  tau1,
  design = c("typeP", "typeM"),
  prop_transfer = 0.5,
  R0_1 = NULL
)
}
\arguments{
\item{n}{Number of units placed on test.}

\item{family}{A family key (see \code{\link[=slt_families]{slt_families()}}) or family object.}

\item{theta0}{Numeric \code{c(lambda0, alpha0)}, baseline-stage parameters.}

\item{theta1}{Numeric \code{c(lambda1, alpha1)}, elevated-stage parameters.}

\item{tau1}{Time at which the transfer decision is made.}

\item{design}{\code{"typeP"} (default, fixed target proportion) or
\code{"typeM"} (fixed target count); see Details.}

\item{prop_transfer}{Proportion (in \verb{[0, 1]}) of survivors at \code{tau1}
transferred to the elevated stage, for \code{design = "typeP"}. Set to
\code{1} for the classical simple-step-stress design.}

\item{R0_1}{Target number of survivors transferred (capped by the
number actually available), for \code{design = "typeM"}.}
}
\value{
A list with \code{Y} (all stage-0 failure times: early failures
before \code{tau1} plus the non-transferred survivors, who keep failing
under stage-0), \code{Z} (stage-1 observed failure times for the
transferred units), \code{n_early} (\code{D1}, number failing before \code{tau1})
and \code{n_transferred} (\verb{R*_1}).
}
\description{
Simulates data from the two-stage life-testing model of Laumen &
Cramer (2019, Procedure 2.3).
}
\details{
At the stage-change time \code{tau1}, \verb{R*_1} of the units that have \emph{not
yet failed} are randomly selected and moved to the elevated stage
(cumulative-exposure shifted); the remaining survivors continue
failing under the original stage-0 conditions and are never observed
under stage 1.

Two rules for generating \verb{R*_1} are supported, following the paper:
\describe{
\item{\code{"typeP"}}{A fixed target \emph{proportion} \code{pi1} of the survivors
is transferred: \verb{R*_1 = floor(pi1 * (n - D1))}, where \code{D1} is the
(random) number of stage-0 failures observed before \code{tau1}.
\code{pi1 = 1} transfers \emph{every} survivor, which is exactly the
classical "simple step-stress" design (Remark 2.2(4) of the
paper) -- no separate function is needed for that special case.}
\item{\code{"typeM"}}{A fixed target \emph{count} \code{R0_1} is transferred, capped
by the number of available survivors: \verb{R*_1 = min(n - D1, R0_1)}.}
}
}
\examples{
set.seed(1)
d <- rslt(100, "weibull", theta0 = c(0.05, 1.5), theta1 = c(0.2, 1.5),
          tau1 = 5, design = "typeP", prop_transfer = 0.5)
str(d)

d_m <- rslt(100, "weibull", theta0 = c(0.05, 1.5), theta1 = c(0.2, 1.5),
            tau1 = 5, design = "typeM", R0_1 = 30)
str(d_m)

# The classical simple-step-stress design (every survivor transferred):
d_full <- rslt(100, "weibull", theta0 = c(0.05, 1.5), theta1 = c(0.2, 1.5),
               tau1 = 5, design = "typeP", prop_transfer = 1)
str(d_full)
}
