% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/confint.R
\name{confint.slt_fit}
\alias{confint.slt_fit}
\title{Asymptotic (Wald) confidence intervals for a fitted two-stage life testing model}
\usage{
\method{confint}{slt_fit}(object, parm, level = 0.95, type = c("lognormal", "normal"), ...)
}
\arguments{
\item{object}{An \code{"slt_fit"} object from \code{\link[=fit_slt]{fit_slt()}}.}

\item{parm}{Optional character vector of parameter names to include
(default: all).}

\item{level}{Confidence level.}

\item{type}{\code{"lognormal"} (default) or \code{"normal"}.}

\item{...}{Currently unused.}
}
\value{
A numeric matrix with one row per parameter and two columns
(lower, upper).
}
\description{
Two Wald-type intervals are available: \code{"normal"} and \code{"lognormal"}.
}
\details{
\code{"lognormal"} (the default) builds the interval on the \code{log(theta)}
scale (where the MLE is asymptotically normal by construction) and
exponentiates, which guarantees a positive interval and is generally
preferred for these positive shape/scale parameters. \code{"normal"}
applies the delta-method standard error directly on the natural
scale and can (rarely) produce a negative lower limit for small
samples.
}
\examples{
set.seed(1)
d <- rslt(200, "weibull", c(0.05, 1.5), c(0.2, 1.5), tau1 = 5, prop_transfer = 1)
fit <- fit_slt(d, "weibull", "shape1", tau1 = 5)
confint(fit)
}
