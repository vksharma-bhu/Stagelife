% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stage-structure.R
\name{slt_reduce_params}
\alias{slt_reduce_params}
\title{Reduce full \verb{(theta0, theta1)} parameters to a case's free parameters}
\usage{
slt_reduce_params(theta0, theta1, case = c("common", "shape1", "shape11"))
}
\arguments{
\item{theta0, theta1}{Numeric \code{c(lambda, alpha)} vectors.}

\item{case}{One of \code{"common"}, \code{"shape1"}, \code{"shape11"}.}
}
\value{
A named numeric vector matching \code{case_info(case)$par_names}.
}
\description{
The inverse of \code{\link[=slt_expand_params]{slt_expand_params()}}; mostly useful for extracting the
"true" parameter vector matching a given case in simulation studies.
For \code{"common"}, \code{theta0[2]} (rather than an average) is used as the
shared shape and a warning is issued if \code{theta0[2] != theta1[2]}.
}
\examples{
slt_reduce_params(c(0.2, 1.5), c(0.4, 1.5), case = "common")
}
