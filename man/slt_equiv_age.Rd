% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stage-structure.R
\name{slt_equiv_age}
\alias{slt_equiv_age}
\title{Equivalent age under the elevated stress}
\usage{
slt_equiv_age(family, tau, theta0, theta1)
}
\arguments{
\item{family}{A family key (see \code{\link[=slt_families]{slt_families()}}) or family object.}

\item{tau}{Non-negative scalar change-point time.}

\item{theta0}{Numeric \code{c(lambda0, alpha0)}, baseline-stage parameters.}

\item{theta1}{Numeric \code{c(lambda1, alpha1)}, elevated-stage parameters.}
}
\value{
The equivalent age (a non-negative scalar).
}
\description{
Under the cumulative exposure model (CEM), a unit that has accumulated
damage equivalent to surviving to time \code{tau} under stage-0 conditions
has, under stage-1 conditions, already "used up" an equivalent age
\code{nu}. This function computes \code{nu}.
}
\details{
The CEM's defining assumption is that only the \emph{total accumulated
damage} determines a unit's remaining life, not the stress history
that produced it. Formalising "equal accumulated damage" as "equal
reliability", a unit surviving to \eqn{\tau} under the stage-0
distribution \eqn{F_0} is equivalent to a unit that has survived to
some age \eqn{\nu} under the stage-1 distribution \eqn{F_1}, where
\eqn{\nu} solves
\deqn{S_1(\nu;\theta_1) = S_0(\tau;\theta_0) \quad\Longleftrightarrow\quad
      F_1(\nu;\theta_1) = F_0(\tau;\theta_0),}
i.e.
\deqn{\nu = \nu(\tau;\theta_0,\theta_1) = Q_1\!\left(F_0(\tau;\theta_0);\ \theta_1\right),}
where \eqn{Q_1 = F_1^{-1}} is the stage-1 quantile function. This is
exactly \code{fam$quantile(fam$cdf(tau, theta0), theta1)} below: plug the
stage-0 cdf at \code{tau} into the stage-1 quantile function. \eqn{\nu}
is the building block for the population functions \code{\link[=pslt]{pslt()}}/\code{\link[=dslt]{dslt()}}/
\code{\link[=qslt]{qslt()}} and for the log-likelihood \code{\link[=loglik_slt]{loglik_slt()}} -- see their
documentation for how it is used.
}
\examples{
slt_equiv_age("weibull", tau = 5, theta0 = c(0.1, 1.5), theta1 = c(0.3, 1.5))
}
\seealso{
\code{\link[=pslt]{pslt()}} for how \code{nu} combines with \code{tau1} into the
population cdf; \code{\link[=loglik_slt]{loglik_slt()}} for the resulting log-likelihood;
\code{\link[=slt_tau]{slt_tau()}} for choosing \code{tau} as a stage-0 quantile.
}
