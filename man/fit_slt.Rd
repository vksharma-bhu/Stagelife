% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{fit_slt}
\alias{fit_slt}
\title{Fit a two-stage life testing model by maximum likelihood}
\usage{
fit_slt(
  data,
  family = "weibull",
  case = c("common", "shape1", "shape11"),
  tau1,
  init = NULL,
  n_starts = 4L,
  bootstrap = FALSE,
  control = list()
)
}
\arguments{
\item{data}{A list with numeric vectors \code{Y} (stage-0 observed failure
times) and \code{Z} (stage-1 observed failure times), as produced by
\code{\link[=rslt]{rslt()}} (or assembled directly from real data).}

\item{family}{One of \code{\link[=slt_families]{slt_families()}}.}

\item{case}{One of \code{"common"}, \code{"shape1"}, \code{"shape11"} (see
\code{\link[=slt_cases]{slt_cases()}}).}

\item{tau1}{The stage-change time, used to shift \code{Z} back to the
stage-1 origin.}

\item{init}{Optional numeric vector of starting values \strong{on the
natural scale}, in the order given by \code{case_info(case)$par_names}
(see \code{\link[=slt_cases]{slt_cases()}} for the parameter order for each case). If \code{NULL}
a data-driven default is used.}

\item{n_starts}{Number of (perturbed) starting values to try; the
converged fit with the highest log-likelihood is returned.}

\item{bootstrap}{If \code{TRUE}, use looser/faster optimiser control
settings appropriate for repeated bootstrap refits.}

\item{control}{Optional list overriding \code{\link[stats:optim]{stats::optim()}} control
parameters for both the Nelder-Mead and BFGS stages.}
}
\value{
An object of class \code{"slt_fit"}, a list with elements including
\code{converged}, \code{family}, \code{case}, \code{tau1}, \code{par} (natural-scale
estimates), \code{se}, \code{theta0}, \code{theta1}, \code{vcov_log}, \code{loglik}, \code{aic},
\code{bic}. If no starting value converges, \code{converged} is \code{FALSE} and
most other elements are absent.
}
\description{
Fit the stage life testing model for given data using the
cumulative-exposure method for one of 12 baseline distributions and
one of three parameter cases.
}
\details{
Estimation uses a Nelder-Mead warm start followed by BFGS (with a
numerical Hessian for inference), repeated from several perturbed
starting values to guard against local optima; the converged fit
with the highest log-likelihood is returned.
}
\examples{
set.seed(1)
d <- rslt(150, "weibull", c(0.05, 1.5), c(0.2, 1.5), tau1 = 5, prop_transfer = 1)
fit <- fit_slt(d, family = "weibull", case = "shape1", tau1 = 5)
summary(fit)
}
