% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/likelihood.R
\name{loglik_slt}
\alias{loglik_slt}
\title{Two-stage cumulative-exposure log-likelihood}
\usage{
loglik_slt(data, family, theta0, theta1, tau1)
}
\arguments{
\item{data}{A list with numeric vectors \code{Y} (stage-0 observed failure
times) and \code{Z} (stage-1 observed failure times), as returned by
\code{\link[=rslt]{rslt()}}.}

\item{family}{A family key (see \code{\link[=slt_families]{slt_families()}}) or family object.}

\item{theta0}{Numeric \code{c(lambda0, alpha0)}, baseline-stage parameters.}

\item{theta1}{Numeric \code{c(lambda1, alpha1)}, elevated-stage parameters.}

\item{tau1}{The stage-change time used to shift \code{Z} back to the
stage-1 time origin.}
}
\value{
The log-likelihood (a scalar), or \code{-Inf} for parameter values
outside the support of the model.
}
\description{
Two-stage cumulative-exposure log-likelihood
}
\examples{
set.seed(1)
d <- rslt(80, "weibull", c(0.05, 1.5), c(0.2, 1.5), tau1 = 5, prop_transfer = 1)
loglik_slt(d, "weibull", c(0.05, 1.5), c(0.2, 1.5), tau1 = 5)
}
